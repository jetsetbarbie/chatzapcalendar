{
  "type": "object",
  "properties": {
    "approve": { "type": "string", "enum": ["do it"] },
    "purpose": { "type": "string" },
    "start": { "type": "string", "format": "date-time" },
    "end": { "type": "string", "format": "date-time" }
  },
  "required": ["approve", "purpose", "start", "end"],
  "additionalProperties": false
}
